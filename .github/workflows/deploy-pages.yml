name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Build site
        env:
          API_URL: ${{ secrets.API_URL }}
          COGNITO_USER_POOL_ID: ${{ secrets.COGNITO_USER_POOL_ID }}
          COGNITO_CLIENT_ID: ${{ secrets.COGNITO_CLIENT_ID }}
          COGNITO_DOMAIN: ${{ secrets.COGNITO_DOMAIN }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
        run: |
          mkdir -p _site _site/formularios _site/v2

          cp frontend/index.html _site/index.html

          # Los formularios que el botón de los correos ofrece descargar.
          # Vivían en CloudFront; si no viajan acá, ese botón se rompe en cada
          # correo que le mandamos a un cliente.
          cp lambda/attachments/*.pdf _site/formularios/

          # El front nuevo, mientras se construye. Misma URL base a propósito:
          # la API tiene una lista blanca de orígenes y un dominio nuevo no
          # podría hablarle hasta que alguien con permisos de API Gateway lo
          # agregue. Como subcarpeta, el origen es el mismo y funciona hoy.
          #
          # Se copia el árbol COMPLETO, no sólo el index: la página carga
          # `estilo/tokens.css` y `dominio.js` por ruta relativa, y copiando
          # sólo el index se publicaría una pantalla sin estilos con un 404
          # que no aparece en ningún log.
          cp -R frontend/v2/. _site/v2/
          rm -rf _site/v2/tests _site/v2/PLAN.md
          find _site/v2 -name '__pycache__' -type d -prune -exec rm -rf {} +

          if [ ! -f _site/v2/index.html ]; then
            echo '<!doctype html><meta charset="utf-8"><title>WatchTower v2</title><p style="font:14px system-ui;padding:2rem">Acá va el front nuevo. Todavía no hay nada publicado.</p>' > _site/v2/index.html
          fi
          python3 -c "
          import json, os
          cfg = {
            'apiUrl':        os.environ['API_URL'],
            'userPoolId':    os.environ['COGNITO_USER_POOL_ID'],
            'clientId':      os.environ['COGNITO_CLIENT_ID'],
            'cognitoDomain': os.environ['COGNITO_DOMAIN'],
            'region':        os.environ['AWS_REGION'],
            'geminiKey':     os.environ.get('GEMINI_KEY', ''),
          }
          open('_site/config.json','w').write(json.dumps(cfg, indent=2))
          # El front nuevo lee su config con ruta relativa, así que necesita
          # la suya en su carpeta.
          open('_site/v2/config.json','w').write(json.dumps(cfg, indent=2))
          "

      - uses: actions/configure-pages@v4
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
