boto3>=1.34.0
openpyxl>=3.1.2
PyYAML>=6.0.1
jinja2>=3.1.3
pandas>=2.0.0
xlsxwriter>=3.1.0
PyMySQL>=1.1.0
# Para la ficha del cliente en PDF (ficha_pdf.py). Arrastra Pillow, que son
# ~33 MB: reportlab lo importa siempre, aunque el PDF sea sólo texto — probado,
# sin PIL no se puede ni importar. El paquete queda en ~153 MB descomprimido,
# contra los 250 MB que permite Lambda.
reportlab>=4.0.0
