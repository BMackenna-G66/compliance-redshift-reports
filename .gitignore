# Local config with secrets (never commit)
frontend/config.json
.env

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform.lock.hcl
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# Lambda build artifacts
lambda/build/
lambda_build/
lambda/*.zip
lambda_package.zip

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local outputs / test data
output/
*.xlsx
*.csv
!sample_*.csv
response.json

# Secrets
.env
.env.local
*.pem
*.key

# ── Embargos: datos personales, NUNCA al repo ──────────────────────────
# Este repositorio es PÚBLICO. Los insumos de oficios judiciales traen
# nombre y documento de decenas de miles de personas (Ley 1581 de 2012),
# y los oficios generados llevan el nombre y la cédula en el propio nombre
# del archivo. Sólo viajan el código y las plantillas en blanco.
lambda/embargos/muestras/
lambda/embargos/salida/
**/DEMO_oficios_clientes/
**/DEMO_oficios_no_clientes/
*_oficios_clientes.zip
*_oficios_no_clientes.zip
resultado_validacion*.xlsx
EMBARGO*.xlsx
Oficio*.pdf
CLIENTE_*.docx
NOCLIENTE_*.docx
build_embargos/
embargos_package.zip
